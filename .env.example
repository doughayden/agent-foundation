### ===========================================
### Required Configuration
### ===========================================

### Required name used as a base for Terraform resources and logs ###
AGENT_NAME=adk-docker-uv

### Required GitHub repository information for Terraform CI/CD setup ###
GITHUB_REPO_NAME=adk-docker-uv
GITHUB_REPO_OWNER=your-github-username-or-org


### ===========================================
### Model Authentication: Choose ONE strategy
### ===========================================

### Strategy 1: Google AI API (Gemini Developer API)
### Get your key from: https://aistudio.google.com/apikey
# GOOGLE_GENAI_USE_VERTEXAI=FALSE
# GOOGLE_API_KEY=your_api_key_here

### Strategy 2: Vertex AI (Google Cloud) - DEFAULT
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1


### ===========================================
### Optional Configuration
### ===========================================

### Logging verbosity (default: INFO)
# LOG_LEVEL=DEBUG

### Server configuration override (defaults: HOST=localhost, PORT=8000)
# HOST=localhost
# PORT=8000

### Enable ADK web UI at http://localhost:8000 (default: false)
# SERVE_WEB_INTERFACE=true

### Session and Memory persistence via Agent Engine (default: in-memory ephemeral service)
### NOTE: Agent Engine instance must be created externally
# AGENT_ENGINE=projects/123/locations/us-central1/reasoningEngines/456

### Artifact storage via GCS bucket (default: in-memory ephemeral service)
### NOTE: GCS bucket must be created externally
# ARTIFACT_SERVICE_URI=gs://your-artifact-storage-bucket

### CORS allowed origins (JSON array string, default: ["http://localhost", "http://localhost:8000"])
# ALLOWED_ORIGINS='["https://your-domain.com", "http://localhost:3000"]'

### Override default agent model (default: gemini-2.5-flash)
# ROOT_AGENT_MODEL=gemini-2.5-flash

### Other optional features
# ADK_SUPPRESS_EXPERIMENTAL_FEATURE_WARNINGS=true
